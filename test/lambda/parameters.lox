// Lambda with multiple parameters.
fun apply(fn, a, b) {
  return fn(a, b);
}

print apply(fun (x, y) { return x + y; }, 3, 4); // expect: 7
print apply(fun (x, y) { return x * y; }, 5, 6); // expect: 30
